# Inbox

Drop anything here: documents, exports, screenshots, voice-memo
transcripts, half-written notes. Format doesn't matter.

Then ask your agent to use the `vault-inbox` skill (or just say "process the inbox").

Files are **moved out** untouched — this folder should normally be empty.
This README is the only permanent resident and is ignored by processing.
