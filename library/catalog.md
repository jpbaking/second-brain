# Library catalog — root

DOX-style root: this file is **navigation only** and stays small. Originals
live in year shards (`library/YYYY/`), and each shard's `catalog.md` holds
the actual per-file entries. Never list files here.

Child catalog line format (counts are verified by `scripts/health.py`):
`- [YYYY](YYYY/catalog.md) — N file(s)`

## Year shards

(none yet)
