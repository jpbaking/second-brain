# Second-brain vault — read the root map first

This repository is an AI-executive-secretary vault. Before acting on anything
in it, read `AGENTS.md` in the repository root and follow it. It carries the
hard invariants (originals under `library/` are never edited; `memory/log.md`
is append-only; every filing is indexed and logged; every fact is dated) and
points to the detailed rules in `rules/shared/`.

Do not act from this pointer alone.
